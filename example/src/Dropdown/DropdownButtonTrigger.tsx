import React from 'react';
import { DropdownButtonTriggerProps } from './type';
import { GestureResponderEvent, TouchableOpacity, View } from 'react-native';

export default function DropdownButtonTrigger(props: { onPress: (event: GestureResponderEvent) => void }) {
  return (props: DropdownButtonTriggerProps) => {


    return <TouchableOpacity>

    </TouchableOpacity>
  };
}
